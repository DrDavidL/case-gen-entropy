[pytest]
# Explicit rather than relying on discovery: the repo root also contains a .venv and a
# node_modules, and "run the tests" should never mean "collect a dependency's suite".
testpaths = tests
addopts = -q
